---
description: Refresh .context/master-test-plan.md from the current state of the project without running a full pipeline stage.
---

Read the current `.context/master-test-plan.md`, the test files under `tests/`, and any recent notes under `docs/Test-Plans/` and `docs/Regression-Runs/`. Update the master test plan's scope, coverage summary, and open-risk sections to reflect what's actually true today. Don't touch the rest of the pipeline — this is a targeted refresh of one file.
