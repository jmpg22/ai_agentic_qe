---
description: Reconcile AGENTS.md against the actual state of .claude/skills/, .claude/agents/, and .agents/project.yaml after any of those change.
---

Compare `AGENTS.md`'s skill table, staff table, tool-resolution table, and context-loading map against what actually exists under `.claude/skills/`, `.claude/agents/`, and `.agents/project.yaml`. Fix any drift — a renamed skill, an added agent, a changed variable — so `AGENTS.md` stays the accurate single source of truth loaded every session.
